# Contributing to Span Chain

1. Open an issue first — describe the problem or feature request.
2. Fork and create a branch: `git checkout -b feat/your-feature`
3. Make sure tests pass: `mix test` (requires PostgreSQL 16)
4. Format code: `mix format`
5. Open a pull request.
